# Lightweight dependencies for inference only
# For devices with 500 MB - 1 GB RAM
# Install with: pip install -r requirements_lite.txt

# ONNX Runtime (CPU only - smallest footprint)
onnxruntime>=1.15.0

# Audio Processing (numpy-based, no PyTorch)
librosa>=0.10.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Arabic text (optional, small)
# arabic-reshaper>=3.0.0
# python-bidi>=0.4.2
